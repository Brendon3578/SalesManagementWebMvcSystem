namespace SalesManagementWebMvcSystem.Models.Enums
{
    public enum SalesStatus : int
    {
        Pending = 0, // Pendente
        Billed = 1, // Faturado
        Canceled = 2 // Cancelado
    }
}
